parameters:
    locale: en_EN

framework:
    default_locale: '%locale%'
    translator:
        paths:
            - '%kernel.project_dir%/translations/'
            - '%kernel.project_dir%/bundles/Cwd/FosUserExtensionBundle/Resources/translations/'
        fallbacks:
            - '%locale%'
