twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        version: 'env(VERSION)'
        admin_skin: skin-green
        avanzu_adminlte_close_alert: true
        theme:
          primaryColor: "bg-blue"
          logo: "logo.png"
    form_themes:
        - 'Layout/form-theme.html.twig'